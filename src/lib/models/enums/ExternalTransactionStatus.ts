export enum ExternalTransactionStatus {
  IN_REVIEW = 'in_review',
  REVIEW_REJECTED = 'review_rejected',
  NEW = 'new',
  PROCESSING = 'processing',
  SCHEDULED = 'scheduled',
  COMPLETED = 'completed',
  FAILED = 'failed',
}
